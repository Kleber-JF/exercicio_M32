import styled from 'styled-components'
import { Container, InputGeral } from '../../styles'

export const ContainerBusca = styled(Container)`
  padding: 0;
  max-width: 1024px;
`

export const InputBusca = styled(InputGeral)`
  width: 30%;
  margin: 0px;
`
